package com.studyspark.verify

/**
 * Stub for the local PC verification service (v1.1).
 *
 * Planned endpoints:
 *  POST /v1/verify-quiz  — run code sandboxes (python/node/gcc) and return pass/fail
 *  GET  /health
 *
 * Point the Android app Settings → "PC verify service URL" at this host once implemented.
 *
 * Run later with a Ktor server; this module is a placeholder so the repo layout is ready.
 */
fun main() {
    println("Study Spark verify-service placeholder. Implement Ktor + sandboxes next.")
}
